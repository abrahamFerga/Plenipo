import { registerRootComponent } from "expo";
import App from "./App";

// Registers the root component with Expo, and sets up the environment for both `expo start` and
// a native build.
registerRootComponent(App);
