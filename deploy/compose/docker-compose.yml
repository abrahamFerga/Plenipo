# Plenipo — single-box deployment (the OpenClaw model: one compose file, pinned tags,
# state in named volumes, `docker compose pull && docker compose up -d` as the upgrade).
# Quickstart and upgrade/backup notes: see README.md next to this file.
name: plenipo

services:
  api:
    image: ${PLENIPO_IMAGE:-plenipo-sample:local}
    build:
      context: ../..
      dockerfile: samples/Plenipo.Sample.Host/Dockerfile
    depends_on:
      postgres:
        condition: service_healthy
      audit-postgres:
        condition: service_healthy
      redis:
        condition: service_started
    environment:
      # Production is the safe default. Development must be selected explicitly and the published
      # port binds to loopback by default so dev-header system-admin auth is not internet-exposed.
      ASPNETCORE_ENVIRONMENT: ${PLENIPO_ENVIRONMENT:-Production}
      Auth__Authority: ${AUTH_AUTHORITY:-}
      Auth__Audience: ${AUTH_AUDIENCE:-}
      # First run only: creates the deployment's first tenant and its operator. Outside Development
      # nothing else does, and permissions resolve only after a tenant does — so without this an empty
      # Production database has nobody who could create one. Inert once an operator exists; remove it
      # after the first successful start. See docs/CONFIGURATION.md.
      Bootstrap__TenantSlug: ${BOOTSTRAP_TENANT_SLUG:-}
      Bootstrap__TenantName: ${BOOTSTRAP_TENANT_NAME:-}
      Bootstrap__AdminEmail: ${BOOTSTRAP_ADMIN_EMAIL:-}
      Bootstrap__AdminSubject: ${BOOTSTRAP_ADMIN_SUBJECT:-}
      ConnectionStrings__plenipo-platform: Host=postgres;Port=5432;Database=plenipo_platform;Username=postgres;Password=${POSTGRES_PASSWORD:?create .env from .env.example}
      ConnectionStrings__plenipo-audit: Host=audit-postgres;Port=5432;Database=plenipo_audit;Username=postgres;Password=${AUDIT_POSTGRES_PASSWORD:?set a distinct audit password in .env}
      ConnectionStrings__plenipo-redis: redis:6379
      Ai__Provider: ${AI_PROVIDER:-Mock}
      Ai__Model: ${AI_MODEL:-gpt-4o-mini}
      AgentSecurity__Provider: ${AGENT_SECURITY_PROVIDER:-None}
      AgentSecurity__Endpoint: ${AGENT_SECURITY_ENDPOINT:-}
      AgentSecurity__ApiKey: ${AGENT_SECURITY_API_KEY:-}
      Rag__Enabled: "true"
      Rag__EmbeddingProvider: ${EMBEDDING_PROVIDER:-Mock}
      Rag__ApiKey: ${RAG_API_KEY:-}
      Skills__Enabled: "true"
      Skills__Path: skills
    ports:
      - "${PLENIPO_BIND_ADDRESS:-127.0.0.1}:${PLENIPO_PORT:-8080}:8080"
    restart: unless-stopped

  postgres:
    # Pinned major with pgvector (RAG). pg17 pairs with the data-volume layout;
    # upgrading the major needs a dump/restore, not just a tag bump.
    image: pgvector/pgvector:pg17
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?create .env from .env.example}
      POSTGRES_DB: plenipo_platform
    volumes:
      - plenipo-pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d plenipo_platform"]
      interval: 5s
      timeout: 5s
      retries: 12
    restart: unless-stopped

  audit-postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${AUDIT_POSTGRES_PASSWORD:?set a distinct audit password in .env}
      POSTGRES_DB: plenipo_audit
    volumes:
      - plenipo-audit-pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d plenipo_audit"]
      interval: 5s
      timeout: 5s
      retries: 12
    restart: unless-stopped

  redis:
    image: redis:7.4-alpine
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - plenipo-redisdata:/data
    restart: unless-stopped

# ALL state lives in these two volumes (plus your .env) — back them up and the
# deployment is portable to any Docker host.
volumes:
  plenipo-pgdata:
  plenipo-audit-pgdata:
  plenipo-redisdata:
